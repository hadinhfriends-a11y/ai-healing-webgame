name: Inject Apps Script bridge

on:
  push:
    branches: [main]
    paths:
      - 'api-bridge.js'
      - '.github/workflows/inject-api-bridge.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Inject bridge script
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = '<script src="api-bridge.js?v=2"></script>'
          new = '<script src="api-bridge.js?v=3"></script>'
          if old in s:
              s = s.replace(old, new)
          elif new not in s:
              s = s.replace('</body>', new + '\n</body>')
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit patched index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Use API bridge v3" || exit 0
          git push
